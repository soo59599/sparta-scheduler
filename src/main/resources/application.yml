spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scheduler
    username: root
    password: "0000"
    diver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true